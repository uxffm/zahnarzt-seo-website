name: Deploy to Hostinger FTP

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build website
      run: npm run build
      
    - name: Deploy to Hostinger FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: 82.25.96.102
        username: u139044504.snow-kudu-883447.hostingersite.com
        password: BananaShake34$
        server-dir: public_html/
        local-dir: ./dist/
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/.env
          **/.env.*
          **/README.md
          **/.github/**
          **/src/**
          **/package*.json
          **/tailwind.config.js
          **/tsconfig.json
          **/astro.config.mjs
